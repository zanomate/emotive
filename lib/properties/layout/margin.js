import { property } from 'utils/index';
import { AUTO } from 'constants';

export const margin = property('margin');
margin.AUTO = margin(AUTO);

export const marginTop = property('marginTop');
marginTop.AUTO = marginTop(AUTO);

export const marginBottom = property('marginBottom');
marginBottom.AUTO = marginBottom(AUTO);

export const marginLeft = property('marginLeft');
marginLeft.AUTO = marginLeft(AUTO);

export const marginRight = property('marginRight');
marginRight.AUTO = marginRight(AUTO);

export const marginBlock = property('marginBlock');
marginBlock.AUTO = marginBlock(AUTO);

export const marginInline = property('marginInline');
marginInline.AUTO = marginInline(AUTO);

export const marginBlockStart = property('marginBlockStart');
marginBlockStart.AUTO = marginBlockStart(AUTO);

export const marginBlockEnd = property('marginBlockEnd');
marginBlockEnd.AUTO = marginBlockEnd(AUTO);

export const marginInlineStart = property('marginInlineStart');
marginInlineStart.AUTO = marginInlineStart(AUTO);

export const marginInlineEnd = property('marginInlineEnd');
marginInlineEnd.AUTO = marginInlineEnd(AUTO);
