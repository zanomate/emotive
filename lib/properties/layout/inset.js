import { property } from 'utils';
import { AUTO } from 'constants';

export const inset = property('inset');
inset.AUTO = inset(AUTO);

export const insetBlock = property('insetBlock');
insetBlock.AUTO = insetBlock(AUTO);

export const insetBlockStart = property('insetBlockStart');
insetBlockStart.AUTO = insetBlockStart(AUTO);

export const insetBlockEnd = property('insetBlockEnd');
insetBlockEnd.AUTO = insetBlockEnd(AUTO);

export const insetInline = property('insetInline');
insetInline.AUTO = insetInline(AUTO);

export const insetInlineStart = property('insetInlineStart');
insetInlineStart.AUTO = insetInlineStart(AUTO);

export const insetInlineEnd = property('insetInlineEnd');
insetInlineEnd.AUTO = insetInlineEnd(AUTO);
