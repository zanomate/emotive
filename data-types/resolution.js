import { measure } from '../utils';
import { DPCM, DPI, DPPX, X } from '../constants';

// Units
export const dpi = measure(DPI);
export const dpcm = measure(DPCM);
export const dppx = measure(DPPX);
export const x = measure(X);
