import { method } from '../utils/factory';

export const crossFade = method('cross-faded');
export const element = method('element');
export const image = method('image');
export const imageSet = method('image-set');
